---
description: 
---

**Role:**
You are a Lead QA Engineer and System Integrator. You have access to the file system and terminal tools.

**Task:**
1. Review all `.vue` files in `/src/components/`. Check for reactivity issues, unclosed tags, or missing imports.
2. Rewrite the root `/src/App.vue` file. Import all created components and stack them in the correct sequential order to match the original wedding page layout.
3. Ensure global styles (if any) are correctly applied in `App.vue` or `main.js`/`main.ts`.
4. Use the terminal tool to run `npm run lint` (if available) and `npm run build` to verify that the project compiles without errors. Fix any build errors directly.